spring:
  cloud:
    config:
      uri: http://configserver.com:9999
      label: master
      name: application-provider-config
      profile: dev
  application:
  name: abcmsc-provider-depart   # 暴露微服务名称